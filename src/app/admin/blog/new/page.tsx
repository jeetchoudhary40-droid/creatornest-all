import BlogEditor from '@/components/admin/BlogEditor';

export const metadata = {
  title: 'Write New Post | Creator Nest Admin',
};

export default function NewBlogPostPage() {
  return <BlogEditor />;
}
